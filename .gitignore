# Local project management file
# This file tracks which projects have ai-config installed locally
# It should NOT be committed to version control
installed-projects.md

# Environment variables with secrets
.env
.env.local
.env.*.local

# OpenCode dependencies and generated files
.opencode/node_modules/
.opencode/package-lock.json
.opencode/.package-lock.json
